Used Prism + Unity.
Created a WPF application that loads modules from the current directory of the app.
The views from every module are loaded into named regions.
The viewModels from every module subscribe and publish a custom event (defined in Common).