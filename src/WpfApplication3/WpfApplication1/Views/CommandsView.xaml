<UserControl x:Class="WpfApplication1.Views.CommandsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" >
  <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Button Content="New" Command="{Binding NewCommand}" Grid.Column="0" Margin="3" />
		<Button Content="Save" Command="{Binding SaveCommand}" Grid.Column="1" Margin="3" />
		<Button Content="Delete" Command="{Binding DeleteCommand}" Grid.Column="2" Margin="3" />
		<Button Content="Exit" Command="{Binding ExitCommand}" Grid.Column="3" Margin="3" />
	</Grid>
</UserControl>
